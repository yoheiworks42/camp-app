<nav class="navbar navbar-fixed-top navbar-inverse">
	<div class="container">
		<div class="navbar-header">
			<button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbarHeader">
				<span class="sr-only">Toggle navigation</span>
				<span class="icon-bar"></span> <!--バーガーの線-->
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/"><i class="fa fa-home">Home</i></a>
		</div>
		
		<div class="collapse navbar-collapse navbar-right" id="navbarHeader">
			<ul class="nav navbar-nav">
				<li>
					<%=link_to ("/index") do%>
						<i class="fa fa-list-ul">一覧</i>
					<%end%>
				</li>
  	 			<li>
  	 				<%=link_to ("#") do%>
  	 					<i class="fa fa-map-marker">地図</i>
  	 				<%end%>
  	 			</li>
  	 			<% if logged_in? %>
    				<li>
    					<%=link_to current_user do%>
    						<i class="fa fa-user"><%=current_user.name%></i>
    					<%end%>
    				</li>
    				<li>
    					<%=link_to logout_path, method: :delete do %>
    						<i class="fa fa-sign-out">ログアウト</i>
    					<%end%>
    				</li>
    			<% else %>
    				<li>
    					<%=link_to ("/signup") do%>
    						<i class="fa fa-sign-in">新規登録</i>
    					<%end%>
    				</li>
					<a class="btn btn-default navbar-btn main_link_btn" href="/login">ログイン</a>
   				 <% end %>
			</ul>
		</div>
	</div>
</nav>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>