<% provide(:title, @user.name)%>
<div class="center">
    <h1>
        <%=gravatar_for @user%></br>
        <%= @user.name%>
    </h1>
    <p>キャンプの写真を投稿するよ</p>
    <%= link_to "編集", "/users/#{@user[:id]}/edit"%>
</div>